import FilterIcon from '../UI/icons/FilterIcon';
import * as Styled from './styles';

export const Filters: React.FC = () => {
  return (
   <Styled.FiltersBox>
    <FilterIcon />
   </Styled.FiltersBox>
  )
}